# 2026-08-11 工作日志

## 个人主页：Django 后端 → 纯静态 GitHub Pages 并上线
- 把原 Python/Django 模板版个人界面重构为纯静态站点（去 `{% %}`/`{{ }}`、内联 SVG/CSS 渐变替代缺失的图片与字体、移除依赖后端的「功能中心」入口、GitHub 链接改为 `maohaotian187`）。
- 新增 `README.md`（部署说明）与 `.nojekyll`。
- 经用户明确授权，用 classic PAT 推送到 `maohaotian187/maohaotian187.github.io`（GitHub Pages 用户页）。
- 远端 main 原有未转换的旧破版（含 `martinmyself.cn`、`ZYYO666`、`{{ TDRZ_views }}`），已强制覆盖为我本地修好的版本；Pages 构建完成，线上已生效（复检 Django 标签/ZYYO666 均 0 残留）。
- 推送后已把 `.git/config` 里的明文 PAT 抹掉，remote URL 回到无令牌的 `https://github.com/maohaotian187/maohaotian187.github.io.git`。
- 本地分支已对齐为 `main` 并跟踪 `origin/main`。
- 提醒用户：用完后去 GitHub 撤销该 PAT（Settings → Developer settings → PAT）。
